# Vetrina natalizia: nessun build, pubblica la pagina statica.
[build]
  command = "echo nessun build necessario"
  publish = "vetrina-natalizia"

# tutte le route servite dalla vetrina
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
